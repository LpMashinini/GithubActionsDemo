
name: env workflow
"on":
  push:
    branches:
      - main

env:
  App_ENV: "dev" #workfow level

jobs:
  env-demo:
    env:
      App_ENV: "test" #job level 
    runs-on: ubuntu-latest
    steps:
      - name: Connecting to Env
        env:
           App_ENV: "production" #step level 
        run: echo "App is connecting to $App_ENV"
      
