
name: hello-world
"on":
  push:
    branches:
      - main

env:
  App_ENV: "dev"
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Connecting to Env
        run: echo "App is connecting to $App_ENV"
      