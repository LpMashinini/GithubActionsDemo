name: Checkout_workflow
"on": push
jobs:
    checkout-demo:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            - name: print index.html file
              run: cat index.html