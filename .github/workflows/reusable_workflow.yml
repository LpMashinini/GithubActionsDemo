name: Reusable workflow
on:
    workflow_call:

jobs:
    greetings:
        runs-on: ubuntu-latest
        steps:
            - name: Greeting step
              run: echo "Welcome to reusable workflow"
