name: Variable workflow
"on":
  push:
    branches:
      - main
      - release/**
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "My name is ${{vars.MY_NAME}} & docker password is ${{secrets.DOCKER_PASSOWRD}}"