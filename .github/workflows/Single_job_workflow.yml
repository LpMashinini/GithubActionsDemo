name: hello-world
"on":
  push:
    branches:
      - main
      - release/**
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
          echo "This is line one!"
          echo "This is line two"
